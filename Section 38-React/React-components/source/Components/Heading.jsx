import React from "react"; //its a react file, so its imported to actually use the import/export functions

function Heading() {
  //Pascal Casing is used. First letter capital
  return <h1>My Favourite Foods</h1>; //a component to get header
}

export default Heading; //exporting the contents of the function
